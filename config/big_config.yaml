catalog:
  db_path: data/big_catalog.db
# Embeddings env vars (same as config/config.yaml) are also respected here.
data_sources:
  - name: big_demo_sqlite
    type: sqlite
    connection: data/big_demo.db
schema_docs_path: config/big_schema_docs.json
